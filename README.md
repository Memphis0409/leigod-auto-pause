# 雷神自动暂停

在 Windows 版雷神加速器退出或 Windows 关机/重启前，调用雷神客户端自身的暂停时长逻辑。

- 不读取、不保存账号或密码。
- 原始 `app.asar` 按 SHA-256 备份，可卸载还原。
- 雷神升级后可在 Codex 中说“修复雷神自动暂停”。
- 账号未登录、网络断开或当前消耗的是不可暂停时长时，只记录状态，不伪造成功。

安装后需重启一次雷神。运行日志位于 `%LOCALAPPDATA%\LeigodAutoPause\auto-pause.log`。
